# frozen_string_literal: true

RSpec.describe Hf::Api do
  it "has a version number" do
    expect(Hf::Api::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
